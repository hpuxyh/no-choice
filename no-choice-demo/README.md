# 不做选择 Web

基于 PRD v1 做的一期交互原型：建局、问题类型识别、滑卡锁定、滑到底兜底拍板、是否题直接结论、三种人格玄学理由。

## 运行

```bash
npm install
npm run dev
```

当前本地预览地址：

```text
http://127.0.0.1:5174/
```

## Demo 边界

- 大模型推荐先用本地规则和候选池模拟。
- 玄学理由使用本地模板库，后续可在 `src/decisionEngine.js` 里替换为后端 AI 接口。
- 餐厅评分、人均、距离为演示字段，未接真实 POI。
